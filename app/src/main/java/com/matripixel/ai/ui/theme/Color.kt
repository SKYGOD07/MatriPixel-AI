package com.matripixel.ai.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * MatriPixel "Health in Pixels" Color Palette
 * Designed for ASHA workers with high contrast for outdoor/low-light visibility
 */

// === Medical Blues - Primary Brand ===
val MedicalBlue = Color(0xFF1565C0)
val MedicalBlueLight = Color(0xFF5E92F3)
val MedicalBlueDark = Color(0xFF003C8F)
val MedicalBlueContainer = Color(0xFFD4E3FF)
val OnMedicalBlueContainer = Color(0xFF001C3B)

// === Emergency Reds - High Risk / Alerts ===
val EmergencyRed = Color(0xFFD32F2F)
val EmergencyRedLight = Color(0xFFEF5350)
val EmergencyRedDark = Color(0xFFB71C1C)
val EmergencyRedContainer = Color(0xFFFFDAD6)
val OnEmergencyRedContainer = Color(0xFF410002)

// === Warning Amber - Moderate Risk ===
val AmberWarning = Color(0xFFFFA000)
val AmberWarningLight = Color(0xFFFFCA28)
val AmberWarningDark = Color(0xFFC67100)
val AmberContainer = Color(0xFFFFE0A0)
val OnAmberContainer = Color(0xFF3D2E00)

// === Safe Green - Low Risk ===
val SafeGreen = Color(0xFF388E3C)
val SafeGreenLight = Color(0xFF66BB6A)
val SafeGreenDark = Color(0xFF1B5E20)
val SafeGreenContainer = Color(0xFFC8E6C9)
val OnSafeGreenContainer = Color(0xFF002204)

// === High Contrast Surfaces ===
val SurfaceLight = Color(0xFFFFFBFE)
val SurfaceContainerLight = Color(0xFFF3EDF7)
val SurfaceDark = Color(0xFF1C1B1F)
val SurfaceContainerDark = Color(0xFF2B2930)

// === Text Colors ===
val OnSurfaceDark = Color(0xFF1C1B1F)
val OnSurfaceLight = Color(0xFFE6E1E5)
val TextMuted = Color(0xFF79747E)

// === Special UI Colors ===
val OverlayBackground = Color(0x80000000) // 50% black for camera overlay
val GuidanceRectangle = Color(0xCCFFFFFF) // 80% white for guidance box border
